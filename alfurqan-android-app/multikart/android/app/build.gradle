plugins {
    id("com.android.application")
    id("kotlin-android")
    id("dev.flutter.flutter-gradle-plugin")
    id("com.google.gms.google-services")
}

android {
    namespace = "ecom.furqanbook.com"
    compileSdk = 36
    ndkVersion = "27.0.12077973"

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_11
        targetCompatibility = JavaVersion.VERSION_11
    }

    kotlinOptions {
        jvmTarget = JavaVersion.VERSION_11.toString()
    }

    defaultConfig {
        // TODO: Specify your own unique Application ID (https://developer.android.com/studio/build/application-id.html).
        applicationId = "ecom.furqanbook.com"
        // You can update the following values to match your application needs.
        // For more information, see: https://flutter.dev/to/review-gradle-config.
        minSdk = 26
        targetSdk = 35
        versionCode = flutter.versionCode
        versionName = flutter.versionName
    }

    buildTypes {
        release {
            signingConfig = signingConfigs.getByName("debug")
            // 17/09 R8-FIX: image_cropper 11.0.0 me consumer proguard rules
            // MISSING hain (upstream ka known bug — unhone master par baad
            // me fix kiya). Hamari keep-rules alag proguard-rules.pro file me hain;
            // default base config ko IMPORTANT: chhedna nahi hai (optimize
            // base add karne se doosre plugins ke naye R8 issues aa sakte
            // the) — isliye sirf apna rules file APPEND karte hain.
            proguardFiles 'proguard-rules.pro'
        }
    }
}

flutter {
    source = "../.."
}

// 17/09 BUILD-HARDENING (future surprise-build-break roko): Google ke naye
// androidx releases kabhi bhi naya AGP maang sakte hain (aaj hi core 1.18.0
// ne AGP 8.9.1 manga aur build toota). In artifacts ko aaj ke RESOLVED+TESTED
// versions par PIN kar rahe hain — Gradle ab inke naye version apne-aap nahi
// uthayega, isliye kal ko "requires AGP x.y" wala naya jhatka nahi lagega.
// (activity 1.12.4 apne saath navigationevent 1.0.2 transitive laata hai,
//  isliye use alag se pin karne ki zaroorat nahi.)
configurations.all {
    resolutionStrategy {
        force 'androidx.core:core:1.18.0'
        force 'androidx.core:core-ktx:1.18.0'
        force 'androidx.activity:activity:1.12.4'
        force 'androidx.activity:activity-ktx:1.12.4'
    }
}

dependencies {
    // 17/09 R8-FIX (LIVE-verified): image_cropper 11.0.0 → uCrop 2.2.11
    // (jitpack) apni BitmapLoadTask me okhttp3.* use karta hai par jitpack
    // POM se okhttp APP ke classpath par NAHI utarta → release R8
    // "Missing class okhttp3.*" fail. uCrop 2.2.11 khud okhttp 5.1.0 par
    // compile/test hota hai (uske root build.gradle, tag 2.2.11 verified)
    // — wahi EXACT version yahan pin kar rahe hain taaki classes waqai
    // classpath par hon (sirf dontwarn chhupaane se zyada solid fix).
    implementation 'com.squareup.okhttp3:okhttp:5.1.0'
}